import { CreaDirettiveDirective } from './crea-direttive.directive';

describe('CreaDirettiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CreaDirettiveDirective();
    expect(directive).toBeTruthy();
  });
});
